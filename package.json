{
  "name": "proto-1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "packageManager": "pnpm@10.12.1",
  "devDependencies": {
    "turbo": "^2.5.5"
  }
}
